아래의 파이썬 코드를 스트림릿(Streamlit) 웹 애플리케이션 코드로 변환해 줘.
입력값(Input)은 스트림릿의 위젯(예: st.text_input, st.number_input, st.selectbox 등)을 사용해 화면에서 받을 수 있게 해줘.

출력값(Output)이나 결과는 print() 대신 st.write(), st.success(), st.dataframe() 등을 사용해 웹 화면에 깔끔하게 보여줘.

코드의 핵심 로직과 기능은 그대로 유지해야 해.
